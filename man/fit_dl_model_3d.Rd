% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{fit_dl_model_3d}
\alias{fit_dl_model_3d}
\title{Train the 3D PointNet model}
\usage{
fit_dl_model_3d(
  model,
  train_input_path,
  epochs = 150L,
  batch_size = 4L,
  lr_rate = 0.001,
  label_smoothing = 0.1
)
}
\arguments{
\item{model}{A model object from \code{get_dl_model_3d()}.}

\item{train_input_path}{Character. Path to training data folder
containing C1..C6 subfolders with .las/.laz files.}

\item{epochs}{Integer. Number of training epochs. Default 150.}

\item{batch_size}{Integer. Batch size. Default 4.}

\item{lr_rate}{Numeric. Learning rate. Default 0.001.}

\item{label_smoothing}{Numeric. Label smoothing factor. Default 0.1.}
}
\value{
Character. Path to the output directory containing saved
  model weights (pointnet_fold1.pt ... pointnet_fold5.pt).
}
\description{
Trains the PointNet model using 5-fold cross-validation on your
labeled tree dataset. Saves trained weights to the output directory.
This is the 3D equivalent of \code{fit_dl_model()}.

Only needs to be run \strong{once} — trained weights are reused for
all future calls to \code{predict_treedamage_3d()}.
}
\examples{
\dontrun{
# Mirrors: weights <- fit_dl_model(model, train_path, test_path, ...)
model   <- get_dl_model_3d(num_classes = 6)
weights <- fit_dl_model_3d(
  model            = model,
  train_input_path = "data/",
  epochs           = 150L,
  batch_size       = 4L,
  lr_rate          = 0.001
)
}

}
\seealso{
\code{\link{get_dl_model_3d}}, \code{\link{predict_treedamage_3d}}
}
